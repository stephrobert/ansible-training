---
# execution-environment.yml : À COMPLÉTER PAR VOS SOINS.
#
# Définition d'un EE custom au schéma ansible-builder v3 :
#   - base : community-ee-minimal (registre officiel),
#   - ansible-core épinglé strictement (==),
#   - dépendances galaxy / python / system branchées sur les fichiers
#     requirements.yml, requirements.txt et bindep.txt (à écrire aussi).
version: ???

images:
  base_image:
    name: ???

dependencies: {}
