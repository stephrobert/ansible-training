---
# .github/workflows/build-ee.yml : À COMPLÉTER PAR VOS SOINS.
#
# Cahier des charges (contrôlé par pytest, voir challenge/README.md) :
#   1. Build de l'EE avec ansible-builder, scan Trivy bloquant
#      (exit-code: '1' sur HIGH/CRITICAL), push, signature cosign keyless.
#   2. Toutes les actions externes épinglées par SHA de 40 caractères.
#      Astuce : gh api repos/<owner>/<repo>/git/refs/tags/<tag> --jq .object.sha
#   3. permissions: {} au global, droits réélargis dans le job
#      (contents: read, packages: write, id-token: write pour cosign).
#   4. persist-credentials: false sur le checkout.
#
# Remplacez chaque `???` et complétez les étapes.

name: Build & publish Execution Environment

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions: ???

jobs:
  build-and-scan:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@???
