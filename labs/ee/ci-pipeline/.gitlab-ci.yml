---
# .gitlab-ci.yml : À COMPLÉTER PAR VOS SOINS.
#
# Pipeline EE équivalent au workflow GitHub Actions, en 4 stages :
#   build (ansible-builder), scan (Trivy bloquant), push (podman),
#   sign (cosign).
#
# Remplacez `???` et écrivez les 4 jobs correspondants.

stages:
  - ???
