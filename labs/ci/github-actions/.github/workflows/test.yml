---
# .github/workflows/test.yml : À COMPLÉTER PAR VOS SOINS.
#
# Cahier des charges (contrôlé par pytest, voir challenge/README.md) :
#   1. Deux jobs : `lint` puis `molecule`, le second ne part que si le
#      premier passe.
#   2. Job lint : yamllint + ansible-lint --profile=production sur roles/.
#   3. Job molecule : matrice `distro` (au moins 2) x `ansible` (au moins 2),
#      fail-fast désactivé pour obtenir le rapport complet.
#   4. Chaque action externe épinglée par SHA complet (40 caractères),
#      commentée avec le tag correspondant.
#      Astuce : gh api repos/actions/checkout/git/refs/tags/v4.2.2 --jq .object.sha
#   5. `permissions:` global au plus strict, droits réélargis job par job.
#   6. `persist-credentials: false` sur chaque checkout.
#
# Remplacez chaque `???` et complétez les jobs.

name: Test webserver role

on:
  push:
    branches: [main]
  pull_request:

permissions: ???

jobs:
  lint:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@???

  molecule:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@???
