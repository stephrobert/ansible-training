---
id: modules-reseau-get-url
title: "get_url module: download a file over HTTP/HTTPS"
description: "Download a file onto the managed node idempotently, check its integrity with a sha256 checksum, and authenticate the request."
section: modules-reseau
level: l1
difficulty: beginner
lab_type: lab
estimated_time: "35m"
skills:
  - get-url
  - checksum
  - http-auth
distros:
  - alma9
doc_url: https://blog.stephane-robert.info/docs/infra-as-code/gestion-de-configuration/ansible/modules/reseau/module-get-url/
certification_tags:
  - rhce
runtime:
  type: vm
  targets:
    - name: rhce
      host: control-node.lab
      label_en: "AlmaLinux 9 control node"
      label_fr: "Control node AlmaLinux 9"
      roles:
        db1: db1.lab
        # Hôte qui sert le dépôt de référence interne du lab (nginx). web2 est
        # le seul managed node dont le nginx.conf est celui du paquet : les
        # labs de la section « roles » remplacent celui de db1 et désinstallent
        # le paquet, et « declaratif-vs-imperatif » fait de même sur web1.
        api: web2.lab
  default: rhce
validation:
  functional: true
  security: false
  persistence_after_reboot: false
