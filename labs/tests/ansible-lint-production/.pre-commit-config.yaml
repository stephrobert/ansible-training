---
# .pre-commit-config.yaml : À COMPLÉTER PAR VOS SOINS.
#
# Attendu (contrôlé par pytest) :
#   - hooks de base (dont detect-private-key contre les fuites de clés),
#   - yamllint branché sur votre .yamllint,
#   - ansible-lint avec le profil production.
repos: []
