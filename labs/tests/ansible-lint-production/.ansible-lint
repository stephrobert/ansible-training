---
# .ansible-lint : À COMPLÉTER PAR VOS SOINS.
#
# Attendu (contrôlé par pytest) :
#   - le profil le plus strict d'ansible-lint,
#   - exclusions raisonnables (.cache/, .git/, molecule/, tests/).
profile: ???

exclude_paths: []
