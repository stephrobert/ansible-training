---
# .yamllint : À COMPLÉTER PAR VOS SOINS.
#
# Attendu (contrôlé par pytest) :
#   - étendre la config par défaut,
#   - règle truthy stricte : seuls true/false sont admis (YAML 1.2),
#   - line-length assoupli à votre convenance (warning).
extends: default

rules: {}
