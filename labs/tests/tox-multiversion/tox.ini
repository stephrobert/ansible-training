; tox.ini : À COMPLÉTER PAR VOS SOINS.
;
; Cahier des charges (contrôlé par pytest, voir challenge/README.md) :
;   1. [tox] envlist avec la syntaxe range ansible-2.{...} couvrant au
;      moins 3 versions d'ansible-core.
;   2. [testenv] générique qui lance `molecule test`.
;   3. Un [testenv:ansible-X] par version, chacun épinglant son
;      ansible-core dans deps.
;   4. Un [testenv:lint] séparé (yamllint + ansible-lint, fail-fast).
;
; Remplacez `???` et ajoutez les sections manquantes.

[tox]
envlist = ???
skipsdist = true

[testenv]
commands = ???
