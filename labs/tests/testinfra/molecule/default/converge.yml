---
- name: Converge
  hosts: all
  become: true
  roles:
    - role: webserver
      vars:
        webserver_listen_port: 8080
        webserver_index_content: "Hello from testinfra-tested webserver"
