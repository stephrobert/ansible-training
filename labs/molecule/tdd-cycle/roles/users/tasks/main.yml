---
# tasks/main.yml : À ÉCRIRE EN SECOND (étape GREEN du cycle TDD).
#
# N'écrivez PAS ce fichier avant verify.yml : le cycle TDD commence par
# des tests rouges. Écrivez ensuite ici le minimum qui les fait passer :
# une boucle sur users_to_create qui crée chaque utilisateur avec son
# shell (fallback users_default_shell) et ses groupes (append).
# Le contrat d'entrée est défini dans meta/argument_specs.yml.
[]
