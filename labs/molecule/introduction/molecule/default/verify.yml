---
# verify.yml : À COMPLÉTER PAR VOS SOINS.
#
# Écrivez des tâches qui PROUVENT l'état de l'instance après converge,
# avec au moins 2 assertions ansible.builtin.assert distinctes :
#   - le paquet nginx est installé,
#   - le fichier /etc/nginx/nginx.conf existe.
# Collectez l'état (command rpm -q, stat...) puis assertez dessus.

- name: Verify
  hosts: all
  gather_facts: false
  tasks: []
